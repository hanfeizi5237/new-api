# AI Token 集市平台 — M1 开发执行 Checklist

> 日期：2026-04-16
> 来源：`03-M1修复清单_20260416.md`
> 目标：把 review 问题拆成可按顺序执行的开发任务

## 使用说明

- 按阶段执行，不建议跳序
- `P0` 未关闭前，不进入 `P1`
- 每完成一个阶段，都执行对应验证命令
- 所有修复都应优先补测试，再改实现

---

## Phase 0：基线确认

- [ ] 确认当前工作分支为 `dev`
- [ ] 记录当前基线 commit：`17df9868`
- [ ] 通读 [03-M1修复清单_20260416.md](/Users/hanfei/Documents/workspace/tokenbank/new-api-main/docs/ai-token-marketplace_planning_20260410/review/03-M1%E4%BF%AE%E5%A4%8D%E6%B8%85%E5%8D%95_20260416.md)
- [ ] 建立本轮修复记录方式：commit 粒度建议按 `P0 / P1 / P2`

**完成标准**

- 修复范围冻结
- 不再新增无关改动

---

## Phase 1：P0 阻断项

### Task 1.1：修复 `lockForUpdate`

**文件**

- `service/marketplace_inventory.go`

**动作**

- [ ] 改正 `lockForUpdate` 实现
- [ ] SQLite 明确跳过 `FOR UPDATE`
- [ ] MySQL / PostgreSQL 保留 `FOR UPDATE`
- [ ] 检查所有依赖该函数的调用点是否仍成立

**建议补测**

- [ ] SQLite 下不附加 `FOR UPDATE`
- [ ] MySQL / PostgreSQL 下附加 `FOR UPDATE`

**完成标准**

- `lockForUpdate` 不再对所有数据库退化为 no-op

### Task 1.2：修复支付成功但发权失败状态保留

**文件**

- `service/market_payment_service.go`
- `service/entitlement_service.go`

**动作**

- [ ] 调整 `CompleteMarketOrderPayment` 事务边界
- [ ] 确保支付成功状态可落库
- [ ] 发权失败时稳定保留 `entitlement_status=failed`
- [ ] 保留 `retryEntitlementGrantOnly` 逻辑
- [ ] 确认不会重复修改库存

**建议补测**

- [ ] 模拟 PSP 回调成功但 `grantEntitlementsForOrderTx` 返回 error
- [ ] 断言订单最终状态为 `paid + entitlement_failed`
- [ ] 重试后只补发 entitlement，不重复累加库存

**完成标准**

- 数据库可稳定保留失败态
- 后续重试能正确恢复

### Task 1.3：限制支付 Webhook body 大小

**文件**

- `controller/market_payment.go`
- `router/api-router.go`

**动作**

- [ ] 为 marketplace webhook 增加 body size 限制
- [ ] 方案二选一：
- [ ] 在 handler 内使用 `http.MaxBytesReader`
- [ ] 或增加统一中间件限制

**建议补测**

- [ ] 超大 body 返回 `413`
- [ ] 正常 webhook 请求不受影响

**完成标准**

- 公开 webhook 端点不再可被超大 body 直接打爆

### Task 1.4：`PayMarketOrder` 增加 `payment_method` 白名单校验

**文件**

- `controller/market_order.go`

**动作**

- [ ] 在 controller 层校验 `payment_method`
- [ ] 仅允许 `epay / stripe / creem / waffo`
- [ ] 非法值直接返回 400

**建议补测**

- [ ] 非法 `payment_method` 返回失败
- [ ] 合法值正常进入 service

**完成标准**

- 非法支付方式不再穿透到 service

**Phase 1 验证命令**

```bash
go test ./service -run 'TestNewBillingSession|TestMarketPayment|TestMarketplace' -count=1
go test ./controller -run 'TestMarket' -count=1
```

---

## Phase 2：P1 核心正确性与账务项

### Task 2.1：回写 `market_order_item.used_amount`

**文件**

- `service/marketplace_entitlement_funding.go`
- `model/market_order.go`

**动作**

- [ ] success settle 时更新 `market_order_item.used_amount`
- [ ] failed / refund 不误写
- [ ] retry 不重复累计

**建议补测**

- [ ] 单次成功结算后 `used_amount` 增加
- [ ] retry 后不重复增加

### Task 2.2：补全 `usage_ledger` usage 明细

**文件**

- `service/marketplace_entitlement_funding.go`
- `model/usage_ledger.go`

**动作**

- [ ] success ledger 写入 `prompt_tokens`
- [ ] success ledger 写入 `completion_tokens`
- [ ] success ledger 写入 `total_tokens`
- [ ] failed ledger 写入 `ledger_status`
- [ ] failed ledger 写入 `error_code`
- [ ] 视需要补充 `other`

**建议补测**

- [ ] success ledger 断言 usage 明细
- [ ] failed ledger 断言错误归属

### Task 2.3：消除事务外重复查询 items

**文件**

- `service/market_payment_service.go`

**动作**

- [ ] 将事务内查询出的 items 保存在局部变量
- [ ] 事务提交后直接复用，不再重新查询

**完成标准**

- `syncMarketplaceInventoryAfterMutation` 不再依赖额外 DB 查询

### Task 2.4：修复 `CreateSellerSecret` 的 not found 错误处理

**文件**

- `service/seller_service.go`

**动作**

- [ ] 对 `gorm.ErrRecordNotFound` 做显式分支
- [ ] seller 不存在时返回清晰业务错误
- [ ] supply 不存在时返回清晰业务错误

### Task 2.5：实现 `sellerSecretLiveProbeFunc`

**文件**

- `service/secret_provider_probe.go`
- `service/secret_verify_service.go`

**动作**

- [ ] 至少实现 OpenAI-compatible 基础探测
- [ ] 不允许 probe 永远 `return nil`
- [ ] 验证失败时阻止 secret 激活

**建议补测**

- [ ] 无效 key 验证失败
- [ ] 有效 key 验证成功

### Task 2.6：统一补 path / query 数字参数校验

**文件**

- `controller/seller.go`
- `controller/listing.go`
- `controller/market_order.go`
- `controller/entitlement.go`
- 以及其他 marketplace controller

**动作**

- [ ] 所有 path/query 数字参数 parse 成功后加 `> 0` 校验
- [ ] `0` / 负数统一返回 400

### Task 2.7：补 `CreateMarketOrderRequest` 正数校验

**文件**

- `controller/market_order.go`

**动作**

- [ ] `listing_id > 0`
- [ ] `sku_id > 0`
- [ ] `quantity > 0`
- [ ] 非法值在 controller 层快速失败

### Task 2.8：修复 entitlement upsert 的 TOCTOU 竞态

**文件**

- `service/entitlement_service.go`

**动作**

- [ ] 处理唯一索引冲突
- [ ] duplicate key 后 re-read 已有 entitlement
- [ ] 保证并发创建时最终只保留一条记录

**建议补测**

- [ ] 并发创建同一 `(buyer_user_id, vendor_id, model_name)` entitlement
- [ ] 最终只有一条记录

**Phase 2 验证命令**

```bash
go test ./service -run 'TestNewBillingSession|TestMarketPayment|TestEntitlement|TestMarketplace' -count=1
go test ./controller -run 'TestSeller|TestListing|TestMarket' -count=1
```

---

## Phase 3：P2 性能与授权补强

### Task 3.1：优化 marketplace 列表 N+1

**文件**

- `controller/listing.go`
- `controller/market_order.go`
- 相关 service/model 批量查询方法

**动作**

- [ ] public listings 改批量 detail 组装
- [ ] admin listings 改批量 sku 查询
- [ ] buyer orders 改批量 items 查询

**完成标准**

- 单页 20 条数据时查询次数明显下降

### Task 3.2：补 Buyer 授权作用域测试

**文件**

- `controller/market_order.go`
- `controller/entitlement.go`

**动作**

- [x] 构造 Buyer A / Buyer B 两组数据
- [x] 验证 Buyer A 只能看到自己的 orders
- [x] 验证 Buyer A 只能看到自己的 entitlements

**当前证据（2026-05-10）**

- [x] `controller/market_order_test.go`：覆盖 Buyer A 列表只返回自己的订单
- [x] `controller/market_order_test.go`：覆盖 Buyer A 访问 Buyer B 订单详情返回 `order does not belong to buyer`
- [x] `controller/entitlement_test.go`：覆盖 Buyer A 列表只返回自己的 entitlement
- [x] `service/entitlement_service_test.go`：补 buyer 维度 entitlement 查询过滤，避免只依赖 controller 断言

**Phase 3 验证命令**

```bash
go test ./controller -run 'TestMarket|TestEntitlement' -count=1
```

**验证结果（2026-05-10）**

- [x] `go test ./controller -run 'TestMarket|TestEntitlement' -count=1`
- [x] `go test ./service -run TestEntitlement -count=1`

---

## Phase 4：P3 工程化与清理项

### Task 4.1：审计模型与更新语义清理

- [ ] `SellerSecretAudit` 去掉不必要的 `UpdatedAt`

### Task 4.2：库存释放更新方式优化

- [ ] `releaseInventoryFreezeTx` 评估从 `Save` 改为 `Updates`

### Task 4.3：测试环境变量补齐

- [ ] 测试中显式设置 `SELLER_SECRET_FINGERPRINT_SALT`

### Task 4.4：前端 i18n 清理

- [ ] 库存标签中文改走 `t()`

### Task 4.5：诊断与健壮性增强

- [ ] `decodeStoredMarketPaymentIntent` 解析失败时补日志
- [ ] `orderCount` 是否需要暴露给调用方
- [ ] 迁移测试是否扩到 MySQL / PostgreSQL
- [ ] `CreateListingAdminRequest` 是否改为 DTO
- [ ] `strconv.Atoi` 错误是否改为显式报错

**当前事实（2026-05-10）**

- [x] SQLite：`go test ./model -run TestMarketplaceMigration -count=1` 已执行通过
- [ ] MySQL：仓库有 `service/marketplace_mysql_acceptance_test.go` 作为可选入口，但本轮未执行
- [ ] PostgreSQL：当前仓库内未发现同等 marketplace acceptance 用例，本轮未执行

---

## 最终回归

- [ ] `go test ./service ./controller ./model -count=1`
- [ ] `go test ./...`
- [ ] 如条件允许，补一次 SQLite 全量 marketplace 验证
- [ ] 如条件允许，补一次 MySQL 或 PostgreSQL 关键事务验证

---

## 关闭条件

- [ ] 全部 `P0` 已关闭
- [ ] 全部 `P1` 已关闭
- [ ] usage 账可追溯
- [ ] 支付成功但发权失败可保留失败态并重放
- [ ] 核心测试通过
- [ ] 再次 review 后无新的阻断问题
